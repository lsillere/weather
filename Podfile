# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'test iOS Ingima' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for test iOS Ingima
  pod 'Alamofire', '~> 4.4'
  pod 'Gloss', '~> 1.2'

  target 'test iOS IngimaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'test iOS IngimaUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
